import { createTheme } from "@mui/material";

const greenTheme = createTheme ({
    palette: {
        mode: "light",
        primary: {
            main: "#1E40AF",
        },
        secondary: {
            main: "#F59E0B",
        },
    },
});

export default greenTheme;