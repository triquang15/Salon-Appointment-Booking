import { createTheme } from "@mui/material";

const greenTheme = createTheme ({
    palette: {
        mode: "light",
        primary: {
            main: "#1b5e20",
        },
        secondary: {
            main: "#EAF0F1",
        },
    },
});

export default greenTheme;