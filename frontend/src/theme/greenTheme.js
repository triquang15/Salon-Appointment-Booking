import { createTheme } from "@mui/material";

const greenTheme = createTheme ({
    palette: {
        mode: "light",
        primary: {
            main: "#62c2ebff",
        },
        secondary: {
            main: "#EAF0F1",
        },
    },
});

export default greenTheme;